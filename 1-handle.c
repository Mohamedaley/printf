#include "main.h"
/**
 * char_handle - a function to handle characters
 * @chr: the character passed;
 * Return: the length;
 */
int char_handle(char chr)
{
	int len = 0;

	len = _putchar(chr);

	return (len);
}
